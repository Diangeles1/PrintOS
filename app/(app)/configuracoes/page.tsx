import EmBreve from '@/app/_components/EmBreve';

export default function ConfiguracoesPage() {
  return (
    <EmBreve
      title="Configurações"
      desc="Empresa, usuários, permissões e preferências."
    />
  );
}
